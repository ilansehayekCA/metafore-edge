{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "metafore/mqtt/registration",
  "title": "Access Controller Registration",
  "description": "Published by Access Controller to telemetry/{tenant_id}/{controller_id}/registration on startup",
  "type": "object",
  "required": ["controller_id", "tenant_id", "timestamp", "version", "capabilities"],
  "properties": {
    "controller_id": { "type": "string" },
    "tenant_id": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" },
    "version": { "type": "string", "description": "Access Controller software version" },
    "capabilities": {
      "type": "array",
      "items": { "type": "string", "enum": ["jdbc", "http", "shell"] },
      "description": "Route types this controller can execute"
    },
    "db_type": { "type": "string", "enum": ["mariadb", "mysql", "postgresql", "oracle", "mssql", "none"] },
    "db_host": { "type": "string" },
    "db_port": { "type": "integer" },
    "db_name": { "type": "string", "description": "Database name on this controller" },
    "tenants": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "minItems": 1,
      "uniqueItems": true,
      "description": "Phase 14.18 — Edge claims to serve these tenant slugs/IDs from one physical process. OPTIONAL; absence means legacy single-tenant mode."
    },
    "runtime": {
      "type": "string",
      "enum": ["docker", "native", "docker-host-network", "unknown"],
      "description": "Phase 14.9 — Edge process topology, auto-detected at startup; informs mount-time host validation. OPTIONAL field; absence is treated as 'unknown' by metafore-core."
    },
    "runtime_hints": {
      "type": "object",
      "additionalProperties": true,
      "description": "Phase 14.9 — Free-form diagnostic context backing the runtime classification."
    }
  },
  "additionalProperties": false
}
